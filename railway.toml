[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn alfred.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production]
numReplicas = 1
